apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "brarcher.fullname" . }}-test-connection"
  labels:
    {{- include "brarcher.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "brarcher.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
